<Window x:Class="Modding_Assistant.MVVM.View.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Modding_Assistant"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:core="clr-namespace:Modding_Assistant.Core"
        xmlns:markup="clr-namespace:Modding_Assistant.MVVM.View.Markup"
        mc:Ignorable="d"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        Height="600" Width="900">
    
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    
    <Border Background="{StaticResource PrimaryBackgroundBrush}" CornerRadius="10">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="MouseLeftButtonDown">
                <i:InvokeCommandAction Command="{Binding MoveWindowCommand}"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition Width="30"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="0.25*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="1" Grid.Column="1" Height="50" Orientation="Horizontal">
                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding SettingsCommand}">
                    <Path Data="{StaticResource SettingsIconGeometry}" Fill="{StaticResource PrimaryForegroundBrush}"
                          Stretch="Uniform"/>
                </Button>
                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding ExportCommand}">
                    <Path Data="{StaticResource ExportIconGeometry}"
                              Fill="{StaticResource PrimaryForegroundBrush}"
                              Stretch="Uniform"/>
                </Button>
                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding LanguageCommand}">
                    <TextBlock Text="{Binding CurrentLanguage}" FontSize="22" FontWeight="Bold" VerticalAlignment="Center" 
                               HorizontalAlignment="Center" Foreground="{StaticResource PrimaryForegroundBrush}"/>
                </Button>
            </StackPanel>
            <TextBlock Grid.Row="0" Grid.Column="2" Text="Mod Assistant" Foreground="{StaticResource PrimaryForegroundBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="22" IsHitTestVisible="False"/>
            <StackPanel Grid.Row="0" Grid.Column="3" Grid.ColumnSpan="2" HorizontalAlignment="Right" VerticalAlignment="Top" Orientation="Horizontal">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding WindowState}" Value="Maximized">
                                <Setter Property="Margin" Value="0 6 6 0"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <Button Style="{StaticResource MinimizeButtonStyle}" Command="{Binding MinimizeCommand}"/>
                <Button Style="{StaticResource MaximizeButtonStyle}" Command="{Binding MaximizeCommand}"/>
                <Button Style="{StaticResource CloseButtonStyle}" Command="{Binding ExitCommand}"/>
            </StackPanel>
            <DataGrid Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" ItemsSource="{Binding ModList}" Style="{StaticResource DataGridStyle}"
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"  CellStyle="{StaticResource DataGridCellStyle}"
                      ContextMenu="{StaticResource DataGridContextMenu}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="№" IsReadOnly="True" Width="*" ElementStyle="{StaticResource CenterTextColumnStyle}">
                        <DataGridTextColumn.Binding>
                            <MultiBinding Converter="{StaticResource RowIndexConverter}">
                                <Binding RelativeSource="{RelativeSource AncestorType=DataGrid}"/>
                                <Binding/>
                            </MultiBinding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="{markup:Localize DataGridHeaderOrder}" IsReadOnly="True" Binding="{Binding Order}"  Width="*" ElementStyle="{StaticResource CenterTextColumnStyle}"/>
                    <DataGridTextColumn Header="{markup:Localize DataGridHeaderName}" Width="4*" ElementStyle="{StaticResource CenterTextColumnStyle}">
                        <DataGridTextColumn.Binding>
                            <Binding Path="Name" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True" NotifyOnValidationError="True">
                                <Binding.ValidationRules>
                                    <core:NotEmptyValidationRule/>
                                </Binding.ValidationRules>
                            </Binding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="{markup:Localize DataGridHeaderVersion}" Width="2*" ElementStyle="{StaticResource CenterTextColumnStyle}">
                        <DataGridTextColumn.Binding>
                            <Binding Path="Version" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True" NotifyOnValidationError="True">
                                <Binding.ValidationRules>
                                    <core:NotEmptyValidationRule/>
                                </Binding.ValidationRules>
                            </Binding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="{markup:Localize DataGridHeaderInstallInstructions}" Width="6*" ElementStyle="{StaticResource CenterTextColumnStyle}">
                        <DataGridTextColumn.Binding>
                            <Binding Path="InstallInstructions" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True" NotifyOnValidationError="True">
                                <Binding.ValidationRules>
                                    <core:NotEmptyValidationRule/>
                                </Binding.ValidationRules>
                            </Binding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="{markup:Localize DataGridHeaderURL}" Binding="{Binding Url}" Width="6*" ElementStyle="{StaticResource CenterTextColumnStyle}"/>
                    <DataGridTextColumn Header="{markup:Localize DataGridHeaderDependencies}" Binding="{Binding Dependencies}" Width="2*" ElementStyle="{StaticResource CenterTextColumnStyle}"/>
                    <DataGridTextColumn Header="{markup:Localize DataGridHeaderModRawName}" Binding="{Binding ModRawName}" Width="4*" ElementStyle="{StaticResource CenterTextColumnStyle}"/>
                    <DataGridTemplateColumn Header="{markup:Localize DataGridHeaderLastUpdated}" Width="2*" >
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding LastUpdated, StringFormat='dd.MM.yyyy'}" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <DatePicker>
                                    <DatePicker.SelectedDate>
                                        <Binding Path="LastUpdated" 
                                                 Mode="TwoWay" 
                                                 UpdateSourceTrigger="PropertyChanged" 
                                                 ValidatesOnDataErrors="True" 
                                                 NotifyOnValidationError="True"
                                                 Converter="{StaticResource DateOnlyToDateTimeConverter}">
                                            <Binding.ValidationRules>
                                                <core:NotEmptyValidationRule/>
                                            </Binding.ValidationRules>
                                        </Binding>
                                    </DatePicker.SelectedDate>
                                </DatePicker>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="{markup:Localize DataGridHeaderDescription}" Width="6*" ElementStyle="{StaticResource CenterTextColumnStyle}">
                        <DataGridTextColumn.Binding>
                            <Binding Path="Description" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True" NotifyOnValidationError="True">
                                <Binding.ValidationRules>
                                    <core:NotEmptyValidationRule/>
                                </Binding.ValidationRules>
                            </Binding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="{markup:Localize DataGridHeaderPotentialIssues}" Binding="{Binding PotentialIssues}" Width="6*" ElementStyle="{StaticResource CenterTextColumnStyle}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
    
</Window>
